// This component has been temporarily simplified.
// It can be reimplemented later.
import type { Theme } from '@/lib/themes'; // Assuming Theme type might still be used or defined minimally

interface ThemeCardProps {
  theme: Theme;
}
export function ThemeCard({ theme }: ThemeCardProps) {
  return null; // Or a very basic placeholder
}
